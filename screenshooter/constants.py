"""
Модуль: constants.py
Описание: Общие константы приложения.
          Содержит пороговые значения (HIT_AREA_PADDING, MIN_RECT_SIZE, MIN_ARROW_LENGTH),
          используемые для определения минимальных размеров и областей захвата.
          Содержит константы режимов обрезки: размеры, цвета, Z-значения, стили.
"""

from PyQt5.QtGui import QColor

# ==============================================================
# Базовые константы (существующие)
# ==============================================================

HIT_AREA_PADDING = 8
MIN_RECT_SIZE = 5
MIN_ARROW_LENGTH = 8

# ==============================================================
# Режим обрезки — размеры
# ==============================================================

CROP_CURSOR_SIZE = 32        # Размер курсора-перекрестия (пиксели)
CROP_LABEL_MARGIN = 10       # Отступ текста разрешения от рамки (пиксели)
CROP_LABEL_PADDING = 6       # Отступ текста внутри фона (пиксели)
CROP_LABEL_FONT_SIZE = 12    # Размер шрифта текста разрешения
CROP_STATUS_FONT_SIZE = 14   # Размер шрифта статусной строки в режиме обрезки
CROP_CURSOR_OUTLINE_WIDTH = 3  # Толщина белой обводки курсора
CROP_CURSOR_LINE_WIDTH = 1   # Толщина чёрной линии курсора

# ==============================================================
# Режим обрезки — Z-значения (слои отрисовки)
# ==============================================================

CROP_OVERLAY_Z = 1000        # Затемнение вокруг рамки
CROP_RECT_Z = 1001           # Рамка обрезки
CROP_LABEL_Z = 1002          # Текст разрешения

# ==============================================================
# Режим обрезки — цвета
# ==============================================================

# Фон редактора в режиме обрезки
CROP_BG_COLOR = QColor(90, 90, 90)

# Затемнение вокруг рамки обрезки
CROP_OVERLAY_COLOR = QColor(0, 0, 0, 120)

# Рамка обрезки
CROP_RECT_COLOR = QColor(0, 120, 215)

# Текст разрешения рядом с рамкой (как у info_widget)
CROP_LABEL_TEXT_COLOR = QColor(51, 51, 51)        # #333
CROP_LABEL_BG_COLOR = QColor(255, 255, 255, 180)  # Белый полупрозрачный

# Курсор-перекрестие
CROP_CURSOR_OUTLINE_COLOR = QColor(255, 255, 255)  # Белая обводка
CROP_CURSOR_LINE_COLOR = QColor(0, 0, 0)            # Чёрная линия

# ==============================================================
# Стили статусной строки
# ==============================================================

STATUS_STYLE_CROP = (
    "background-color: rgba(0, 0, 0, 180); color: white; "
    "font-size: 14px; font-weight: bold; "
    "border-radius: 4px; padding: 4px 8px;"
)

STATUS_STYLE_NORMAL = (
    "background-color: rgba(255,255,255,180); color: #333; "
    "border-radius: 6px; padding: 4px 8px;"
)