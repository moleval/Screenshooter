{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Screenshooter (multi-window)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
