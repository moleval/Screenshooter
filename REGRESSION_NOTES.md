# Regression Notes

## Pre-existing defects (не регрессия UI-рефакторинга)

### PE-001: Ошибка совместимости PyQt5/numpy в blur_region
- Обнаружен: Шаг 8, 31.08.2026
- Воспроизведение: на коммитах до 5fc1939 вызвать размытие области
- Причина: использование устаревшего метода sip.voidptr.setArraySize
- Статус: исправлен ранее (замена на asstring), не является регрессией UI-рефакторинга

### PE-002: Размытие выходит за пределы рамки зоны
- Обнаружен: Шаг 8, 31.08.2026
- Воспроизведение: создать зону размытия, сравнить границы рамки и фактическое размытие
- Причина: запас в blur_region (margin = int(radius * 2)) не учитывается при отображении рамки
- Статус: задокументирован, не исправляется в Шаге 8

### PE-003: Фокус в диалоге «Очистить сцену» не на кнопке «Да»
- Обнаружен: Шаг 8, 31.08.2026
- Воспроизведение: открыть диалог очистки сцены, фокус может быть не на «Да»
- Причина: требуется явная установка фокуса
- Статус: задокументирован, не исправляется в Шаге 8

### PE-004: Групповое перетаскивание не работает при захвате за зону размытия
- Обнаружен: Шаг 6 рефакторинга мыши, 01.09.2026
- Воспроизведение: выделить несколько объектов (аннотации, картинки, зону размытия), попытаться перетащить всю группу, схватив за зону размытия. Фактически перетаскивается только зона размытия, выделение снимается с остальных.
- Причина: `ManipulationController._handle_blur_region_press` обрабатывает зону размытия без учёта множественного выделения; приоритет `blur_outside` выше групповой манипуляции.
- Статус: pre-existing, не исправляется в рамках рефакторинга мыши. Запланировано исправить в задаче «Унификация манипуляций».

## Технический долг рефакторинга мыши

### TD-001: Приватные обращения MouseInteractionManager к EditorView
- Обнаружен: Шаг 8
- Список обращений:
  - `view._is_background_item()`
  - `view._item_for_manipulation()`
  - `view._is_point_inside_background()`
  - `view._deactivate_active_text()`
  - `view.temp_item`
  - `view.start_point`
  - `view._tool`
  - `view._first_click_after_activation`
  - `view.active_text_item`
  - `manipulation_controller._drag_items`
  - `manipulation_controller._restore_tool_if_needed()`
- Статус: задокументирован, решение отложено. Возможное решение — выделение публичного API между EditorView и MouseInteractionManager.

## Связанные визуальные исправления (после рефакторинга мыши)

- **StatusBarManager**: тематические стили, принудительная перерисовка после смены стиля.

## Annotation handles baseline

Дата: 2026-09-23
Ветка: `feature/annotation-handles`
Базовый commit: `ede6cc4366d53e7ea4d624aed150a79d4d662c3c`
Версия: 0.8.0

### Зафиксировано до внедрения
- Ручки вставленных изображений работают отдельно.
- Ручки blur-зон работают отдельно.
- Crop/blur имеют собственную обработку мыши.
- Обычное перемещение аннотаций выполняется через `ManipulationController`.
- ТЗ: `docs/annotation_handles_spec.md`.

### Автотесты
В доступном GitHub-контексте отдельный набор тестов для annotation handles не обнаружен. Перед каждым следующим этапом нужно проверять локальные тесты, если они есть в рабочей копии, затем собирать приложение и выполнять ручной сценарий текущего этапа.

### Ручной regression checklist
- одиночное выделение;
- множественное выделение;
- обычное перемещение аннотаций;
- resize вставленных изображений;
- resize blur-зон;
- crop/blur;
- Undo/Redo;
- экспорт без служебных ручек.
